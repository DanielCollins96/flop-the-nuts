{
  "name": "flop-the-nuts",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node ./test-evaluator.js",
    "deploy": "wrangler pages deploy . --project-name flop-the-nuts-wrangler --branch main"
  }
}
